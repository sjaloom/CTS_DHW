within ;
model example
  Modelica.Blocks.Examples.PID_Controller pID_Controller
    annotation (Placement(transformation(extent={{-26,16},{-6,36}})));
  annotation (uses(Modelica(version="4.0.0")));
end example;
